name: Test CodeQL SAST Action

on:
  workflow_dispatch:

permissions:
  security-events: write

jobs:
  test-action-java:
    name: Test CodeQL SAST Action (Java)
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4
        with:
          path: main

      - name: Checkout Jackson-Databind Repository
        uses: actions/checkout@v4
        with:
          repository: seansmith39/H6060-Java-Jackson-Databind
          ref: 1acb8ffe6eb20005114bfe18cfd5809b443789c2
          path: jackson-databind

      - name: Run CodeQL (SAST)
        uses: ./main/.github/actions/sast/codeql
        with:
          language: java
          path: jackson-databind
          artifact-name: sast-codeql-report-java

  test-action-python:
    name: Test CodeQL SAST Action (Python)
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4
        with:
          path: main

      - name: Checkout Pillow Repository
        uses: actions/checkout@v4
        with:
          repository: seansmith39/H6060-Python-Pillow
          ref: 722b5d3350fbbb599fd3966e08e55547a2656cd7
          path: pillow

      - name: Run CodeQL (SAST)
        uses: ./main/.github/actions/sast/codeql
        with:
          language: python
          path: pillow
          artifact-name: sast-codeql-report-python

  test-action-javascript:
    name: Test CodeQL SAST Action (JavaScript)
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4
        with:
          path: main

      - name: Checkout Lodash Repository
        uses: actions/checkout@v4
        with:
          repository: lodash/lodash
          ref: 1144918f3578a84fcc4986da9b806e63a6175cbb
          path: lodash

      - name: Run CodeQL (SAST)
        uses: ./main/.github/actions/sast/codeql
        with:
          language: javascript
          path: lodash
          artifact-name: sast-codeql-report-javascript
